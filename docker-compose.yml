version: "2"
services:
  dbase:
    #image: mysql:8
    #image: mysql:5.7
    image: mysql:5.7
    container_name: mysqlmonitoria
    volumes:
      - ~/mysql/monitoria:/var/lib/mysql
    environment:
      - MYSQL_DATABASE=$BANCO
      - MYSQL_ROOT_PASSWORD=$SENHA
    ports:
      - 3306:3306
    restart: "always"
  apache:
    container_name: appmonitoria
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/var/www/html/
    ports:
      - $WEBPORT:80
    depends_on:
      - dbase 
    restart: "always"
